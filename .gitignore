# ── HASSAS: Patron brief / kaynak dokümanlar ASLA commit edilmez ──
# Repo PUBLIC. Kaynaklar masaüstünde durur; projeye kopyalanırsa docs/brief/ altına
# konur ve bu klasör tamamen ignore'dur.
docs/brief/
*.docx
*.doc
*.pdf

# Patron ekran kaydından çıkarılan revizyon paketi (deşifre metni + ekran
# görüntüleri + talimat dosyası). Yerelde durur, agent okur, commit EDİLMEZ.
docs/gavia-crm-revizyon/

# Brief'ten türetilen iç planlama dokümanları da public repoya girmez
tasks/

# QA ekran görüntüleri (yerel öz-doğrulama çıktıları)
docs/screenshots/

# G-9 tarama ÇIKTISI yerelde kalır — betiğin kendisi (tools/g9-tarama.cjs) commit edilir,
# üretilen rapor edilmez (her çalıştırmada değişir, incelemesi terminalde yapılır).
tools/g9-sonuc.json
tools/canli-sonuc.json

# Claude Code yerel ayarları
.claude/settings.local.json

# macOS / editör
.DS_Store
Thumbs.db
*.swp

# Geçici / scratch
_scratch/
*.log

# Yerel QA araçları — node_modules dadamutfak'a symlink (playwright).
# package.json artık ÜRÜN manifestidir ve commit EDİLİR (KARARLAR.md K-002:
# sıfır npm bağımlılığı, yalnız node:* yerleşikleri; kurulum adımı yok).
node_modules

# Çalışma zamanı verisi ve yerel çıktı (uygulama üretir, kaynak değil)
veri/
*.sqlite
*.sqlite-*
cikti/

# Faz 1: QA ekran görüntüleri ve yerel çalışma verisi
docs/screenshots/
veri/
